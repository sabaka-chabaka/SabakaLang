void say_hello() {
    while (true)
    {
        print("hello");
    }
}

void main() {
    print("Main thread: starting...");
    var t = spawn say_hello;
    print("Main thread: spawned thread");
    join t;
    print("Main thread: thread joined. Bye!");
}

main();
